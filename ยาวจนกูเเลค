local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/NICKISBAD/Nick-s-Modded-KAVO-Lib/main/Nick'sModdedKavoLib.lua"))()

local Window = Library.CreateLib("Control Player UI", "DarkTheme")
local Tab = Window:NewTab("Main")
local Section = Tab:NewSection("Control")
local Players = game:GetService("Players")
local selectedPlayer = nil
local function GetPlayersList()
local list = {}

for _, p in pairs(Players:GetPlayers()) do  
    if p ~= Players.LocalPlayer then  
        table.insert(list, p.Name)  
    end  
end  

return list

end
Section:NewDropdown("เลือกผู้เล่น", "เลือกเป้าหมาย", GetPlayersList(), function(option)

selectedPlayer = option  
print("เลือก:", selectedPlayer)

end)
local function GetPlayersList()
    local list = {}

    for _, p in pairs(game:GetService("Players"):GetPlayers()) do
        if p ~= game.Players.LocalPlayer then
            table.insert(list, p.Name)
        end
    end

    return list
end

local Dropdown = Section:NewDropdown(
    "เลือกผู้เล่น",
    "เลือกเป้าหมาย",
    GetPlayersList(),
    function(option)
        selectedPlayer = option
        print(option)
    end
)

-- ปุ่มรีชื่อ
Section:NewButton("รีชื่อผู้เล่น", "รีเฟรชรายชื่อ", function()
    Dropdown:Refresh(GetPlayersList())
end)
Section:NewButton("ใช้ Tackle ใส่เป้าหมาย", "กดเพื่อโจมตี", function()

if not selectedPlayer then  
    warn("ยังไม่ได้เลือกผู้เล่น")  
    return  
end  

local target = Players:FindFirstChild(selectedPlayer)  

if target and target.Character then  

    local args = {  
        "Tackle",  
        target.Character,  
        "AttackTitan",  
        target.Name,  
    }  

    game:GetService("ReplicatedStorage").Main:FireServer(unpack(args))  
end

end)
Section:NewButton("ใช้ SpinSlash ใส่เป้าหมาย", "กดเพื่อโจมตี", function()

if not selectedPlayer then  
    warn("ยังไม่ได้เลือกผู้เล่น")  
    return  
end  

local target = Players:FindFirstChild(selectedPlayer)  

if target and target.Character then  

    local args = {  
        "SpinSlash",  
        target.Character,  
        "AttackTitan",  
        target.Name,  
    }  

    game:GetService("ReplicatedStorage").Main:FireServer(unpack(args))  
end

end)
Section:NewButton("ใช้ Berserk ใส่เป้าหมาย", "กดเพื่อโจมตี", function()

if not selectedPlayer then  
    warn("ยังไม่ได้เลือกผู้เล่น")  
    return  
end  

local target = Players:FindFirstChild(selectedPlayer)  

if target and target.Character then  

    local args = {  
        "Berserk",  
        target.Character,  
        "AttackTitan",  
        target.Name,  
    }  

    game:GetService("ReplicatedStorage").Main:FireServer(unpack(args))  
end

end)
Section:NewButton("ใช้ Roar ใส่เป้าหมาย", "กดเพื่อโจมตี", function()

if not selectedPlayer then  
    warn("ยังไม่ได้เลือกผู้เล่น")  
    return  
end  

local target = Players:FindFirstChild(selectedPlayer)  

if target and target.Character then  

    local args = {  
        "Roar",  
        target.Character,  
        "AttackTitan",  
        target.Name,  
    }  

    game:GetService("ReplicatedStorage").Main:FireServer(unpack(args))  
end

end)
Section:NewButton("ใช้ GroundSpikes ใส่เป้าหมาย", "กดเพื่อโจมตี", function()

if not selectedPlayer then  
    warn("ยังไม่ได้เลือกผู้เล่น")  
    return  
end  

local target = Players:FindFirstChild(selectedPlayer)  

if target and target.Character then  

    local args = {  
        "GroundSpikes",  
        target.Character,  
        "WarhammerTitan",  
        target.Name,  
    }  

    game:GetService("ReplicatedStorage").Main:FireServer(unpack(args))  
end

end)
Section:NewButton("ใช้ Heal ใส่เป้าหมาย", "กดเพื่อโจมตี", function()

if not selectedPlayer then  
    warn("ยังไม่ได้เลือกผู้เล่น")  
    return  
end  

local target = Players:FindFirstChild(selectedPlayer)  

if target and target.Character then  

    local args = {  
        "Heal",  
        target.Character,  
        "AttackTitan",  
        target.Name,  
    }  

    game:GetService("ReplicatedStorage").Main:FireServer(unpack(args))  
end

end)
Section:NewButton("ใช้ StackSteam ใส่เป้าหมาย", "กดเพื่อโจมตี", function()

if not selectedPlayer then  
    warn("ยังไม่ได้เลือกผู้เล่น")  
    return  
end  

local target = Players:FindFirstChild(selectedPlayer)  

if target and target.Character then  

    local args = {  
        "StackSteam",  
        target.Character,  
        "AttackTitan",  
        target.Name,  
    }  

    game:GetService("ReplicatedStorage").Main:FireServer(unpack(args))  
end

end)
Section:NewButton("ใช้ Hardening ใส่เป้าหมาย", "กดเพื่อโจมตี", function()

if not selectedPlayer then  
    warn("ยังไม่ได้เลือกผู้เล่น")  
    return  
end  

local target = Players:FindFirstChild(selectedPlayer)  

if target and target.Character then  

    local args = {  
        "Hardening",  
        target.Character,  
        "AttackTitan",  
        target.Name,  
    }  

    game:GetService("ReplicatedStorage").Main:FireServer(unpack(args))  
end

end)
Section:NewButton("ใช้ RockThrow ใส่เป้าหมาย", "กดเพื่อโจมตี", function()

if not selectedPlayer then  
    warn("ยังไม่ได้เลือกผู้เล่น")  
    return  
end  

local target = Players:FindFirstChild(selectedPlayer)  

if target and target.Character then  

    local args = {  
        "RockThrow",  
        target.Character,  
        "BeastTitan",  
        target.Name,  
    }  

    game:GetService("ReplicatedStorage").Main:FireServer(unpack(args))  
end

end)
Section:NewButton("ใช้ LongRangedGroundSpike ใส่เป้าหมาย", "กดเพื่อโจมตี", function()

if not selectedPlayer then  
    warn("ยังไม่ได้เลือกผู้เล่น")  
    return  
end  

local target = Players:FindFirstChild(selectedPlayer)  

if target and target.Character then  

    local args = {  
        "LongRangedGroundSpike",  
        target.Character,  
        "WarhammerTitan",  
        target.Name,  
    }  

    game:GetService("ReplicatedStorage").Main:FireServer(unpack(args))  
end

end)
Section:NewButton("ใช้ Stomp ใส่เป้าหมาย", "กดเพื่อโจมตี", function()

if not selectedPlayer then  
    warn("ยังไม่ได้เลือกผู้เล่น")  
    return  
end  

local target = Players:FindFirstChild(selectedPlayer)  

if target and target.Character then  

    local args = {  
        "Stomp",  
        target.Character,  
        "WarhammerTitan",  
        target.Name,  
    }  

    game:GetService("ReplicatedStorage").Main:FireServer(unpack(args))  
end

end)

