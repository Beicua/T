local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/NICKISBAD/Nick-s-Modded-KAVO-Lib/main/Nick'sModdedKavoLib.lua"))()

local Window = Library.CreateLib("Demo UI", "DarkTheme")
local Tab = Window:NewTab("Main")
local Section = Tab:NewSection("System")
local Players = game:GetService("Players")
local selectedPlayer = nil
local function GetPlayersList()
local list = {}

for _, p in pairs(Players:GetPlayers()) do  
    if p ~= Players.LocalPlayer then  
        table.insert(list, p.Name)  
    end  
end  

return list

end
Section:NewDropdown("เลือกผู้เล่น", "เลือกเป้าหมาย", GetPlayersList(), function(option)

selectedPlayer = option  
print("เลือก:", selectedPlayer)

end)
Section:NewButton("ใช้ Tackle ใส่เป้าหมาย", "กดเพื่อโจมตี", function()

if not selectedPlayer then  
    warn("ยังไม่ได้เลือกผู้เล่น")  
    return  
end  

local target = Players:FindFirstChild(selectedPlayer)  

if target and target.Character then  

    local args = {  
        "Tackle",  
        target.Character,  
        "AttackTitan",  
        target.Name,  
    }  

    game:GetService("ReplicatedStorage").Main:FireServer(unpack(args))  
end

end)
